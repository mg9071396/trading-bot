# EGX Algorithmic Trading System - Requirements

# Core Dependencies
pandas>=2.0.0
numpy>=1.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Data Handling
yfinance>=0.2.28

# Machine Learning
xgboost>=2.0.0
scikit-learn>=1.3.0

# Backtesting (optional - has fallback)
# vectorbt>=0.25.0  # Optional, requires additional setup

# NLP for Arabic Sentiment Analysis (optional)
# transformers>=4.30.0  # For AraBERT
# torch>=2.0.0  # PyTorch for transformers

# Audio/Video Transcription (optional)
# openai-whisper>=20231117  # For alternative data transcription

# Visualization (optional)
# matplotlib>=3.7.0

# Logging and Utilities
# Already included: logging (stdlib)

# Development & Testing
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.1.0
mypy>=1.5.0

# Jupyter (optional, for research)
# jupyter>=1.0.0
# ipykernel>=6.25.0

# Note on Optional Dependencies:
# -------------------------------
# The system is designed to work with minimal dependencies for core functionality.
# Optional features (Arabic NLP, Whisper transcription, advanced backtesting)
# will gracefully degrade if their dependencies are not installed.
#
# For production use with all features:
#   pip install -r requirements.txt
#   pip install transformers torch openai-whisper vectorbt
#
# For basic testing:
#   pip install pandas numpy pydantic python-dotenv yfinance xgboost scikit-learn
